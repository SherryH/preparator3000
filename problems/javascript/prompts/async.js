'use strict'

const REPLACE_ME = null;

// For each snippet of code predict the value logged 
// inside of the second setTimeout callback

module.exports = {

/*
var async1 = 0;
for (var i = 10; i < 13; i++) {
  setTimeout(() => { async1 += i; }, i);
}
setTimeout(()=> {
  console.log(async1);
  done();
}, 200);
*/

async1: REPLACE_ME,

/*
let async2 = 0;
for (let i = 10; i < 13; i++) {
  setTimeout(() => { async2 += i; }, i);
}
setTimeout(()=> {
  console.log(async2);
  done();
}, 200);
*/

async2: REPLACE_ME

};
