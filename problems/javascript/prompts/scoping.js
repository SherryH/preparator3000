'use strict'

const REPLACE_ME = null;

// Read through each code snnipet and use your knowledge of javascript 
// scoping/closures to predict the values that are logged at the end

module.exports = {
	
	/*
	var initialValue = 0;

	for(var i = 0; i < 1; i++) {
	  let value = initialValue + 1;
	}

	if(1 == 1) {
	  var value = value || -1;
	  value++;
	}

	(function changeValue() {
	  if(1 == 1) {
	    var value = 2;
	  }
	  value = 3;
	})()

	console.log(value);
	*/

	value: REPLACE_ME,

	/*
	var foo = () => {
	  var a = 5;
	  return function bar() {
	    return a + 10;
	  };
	};
	console.log(foo()());
	*/

	doubleFoo: REPLACE_ME,

};
